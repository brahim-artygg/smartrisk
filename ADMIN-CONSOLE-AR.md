# SmartRisk v0.9 — Admin Console

لوحة الإدارة محمية بدور `admin`، ولا يمكن الوصول إلى API الخاصة بها باستخدام حساب مستخدم عادي. يتم حماية جميع طلبات التعديل عبر `X-CSRF-Token` مرتبط بجلسة المشرف.

## Bootstrap أول مشرف

عند التشغيل يمكن إنشاء/ترقية حساب إداري عبر متغيري البيئة:

```text
SMARTRISK_ADMIN_EMAIL=admin@example.com
SMARTRISK_ADMIN_PASSWORD=<strong password>
SMARTRISK_ADMIN_CSRF_SECRET=<random secret>
```

إذا كان البريد موجودًا مسبقًا، يجب أن تطابق كلمة المرور المخزنة كلمة bootstrap قبل رفع الدور إلى `admin`.

## ما يمكن التحكم به

- الحسابات: البحث، التعليق، التفعيل، soft-delete، الأدوار، والاشتراكات.
- API Keys: تعطيل، إعادة تفعيل المفتاح المعطّل، أو إلغاء المفتاح نهائيًا.
- الخطط: السعر USDT، quota الشهري، حجم الدفعة، RPS، concurrency، وعدد الدفعات النشطة.
- الاشتراكات: الخطة، الحالة، وتاريخ الانتهاء.
- المدفوعات: تسجيل USDT، الشبكة، tx hash، invoice، وتأكيد/فشل العملية. تأكيد الدفع يفعّل الاشتراك لمدة 30 يومًا للخطة المحددة.
- Coupons: خصم نسبي أو ثابت، حدود الاستخدام، الخطة المستهدفة، وتاريخ الانتهاء.
- Grants: منح مؤقتة مع سبب واضح، مع حفظ الاشتراك السابق واستعادته عند إلغاء المنحة.
- Ads & Promotions: banner/homepage/developer/results، الجمهور، الأولوية، CTA وجدولة الانتهاء.
- Platform settings: maintenance mode، public scans، registration، developer API، والإعلانات.
- Audit log: كل تعديل إداري يسجل الفاعل والهدف والوقت والتفاصيل.

لا يتم تخزين مفاتيح API السرية أو كلمات المرور بصورتها الصريحة.
